{
  "name": "패키지 관리와 소프트웨어 설치",
  "concepts": [
    "RPM 계열(<code>rpm -qf</code>=파일 소속 패키지 조회, <code>yum</code>/<code>dnf</code>) vs 데비안 계열(<code>dpkg</code>, <code>apt-get</code> — purge는 설정파일까지 삭제, remove는 보존).",
    "소스 설치: <code>configure → make → make install</code>, <code>make clean</code>은 빌드 산출물 정리."
  ]
}
